import Layout from "@/components/Layout";
import TodosHome from "@/widgets/TodosHome";

export const Home = () => {
  return (
    <Layout>
      <TodosHome />
    </Layout>
  );
};

export default Home;
